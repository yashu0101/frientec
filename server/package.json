{
  "name": "@frientec/server",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "//": "The AI SDK is optional and loaded lazily — the API boots and the studio drafts offline without it.",
  "optionalDependencies": {
    "@anthropic-ai/sdk": "^0.116.0"
  }
}
